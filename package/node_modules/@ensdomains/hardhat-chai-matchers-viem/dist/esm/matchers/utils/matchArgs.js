import { equals } from "@vitest/expect";
import { anyValueSymbol } from "../../constants.js";
const anyValueTester = (a, b) => {
    if (a === anyValueSymbol)
        return true;
};
export const withAnyValue = (expectedArgs, actualArgs) => {
    if (!actualArgs)
        return;
    for (let i = 0; i < expectedArgs.length; i++) {
        if (expectedArgs[i] === anyValueSymbol)
            expectedArgs[i] = actualArgs[i];
    }
};
export const matchArgs = (expectedArgs, actualArgs) => {
    if (!actualArgs)
        return false;
    if (expectedArgs.length !== actualArgs.length)
        return false;
    const equalsResult = equals(expectedArgs, actualArgs, [anyValueTester]);
    return equalsResult;
};
//# sourceMappingURL=matchArgs.js.map