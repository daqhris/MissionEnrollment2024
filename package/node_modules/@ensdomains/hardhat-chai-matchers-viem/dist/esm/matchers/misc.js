import { isAddressEqual } from "viem";
import { TO_EQUAL_ADDRESS_MATCHER } from "./constants.js";
import { getObject } from "./utils.js";
export function supportMisc(Assertion) {
    Assertion.addMethod(TO_EQUAL_ADDRESS_MATCHER, function (expected) {
        const actual = getObject(this);
        const equal = isAddressEqual(actual, expected);
        return this.assert(equal, `expected ${actual} to equal address ${expected}`, `expected ${actual} not to equal address ${expected}`, expected, actual);
    });
}
//# sourceMappingURL=misc.js.map